/** Backend registration API (GET returns { success, count, data }, POST creates team). */
export const REGISTER_API_URL =
  'https://backend-hackathone-qwy4n231o-mqts-projects.vercel.app/api/register';

/** Maximum team registrations before registration closes. */
export const MAX_TEAM_SLOTS = 90;
